{
  "name": "ychang-sound",
  "displayName": "YChang Sound",
  "description": "6",
  "version": "2.0.3",
  "publisher": "Rickyeeeeee",
  "icon": "assets/cgv.jpg",
  "repository": {
    "type": "git",
    "url": "https://github.com/Rickyeeeeee/YChang-Sound.git"
  },
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "YChang Sound",
      "properties": {
        "ychang-sound.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable the error sound on terminal command failure."
        },
        "ychang-sound.randomize": {
          "type": "boolean",
          "default": true,
          "description": "Randomly choose one of the bundled YChang sounds when a terminal command fails."
        },
        "ychang-sound.soundFile": {
          "type": "string",
          "enum": [
            "YChang-bill.mp3",
            "YChang-jdc.mp3",
            "YChang-sunny.mp3",
            "YChang-yaoting.mp3"
          ],
          "default": "YChang-sunny.mp3",
          "description": "Select a specific YChang sound. Used only when randomize is disabled."
        },
        "ychang-sound.volume": {
          "type": "number",
          "default": 1,
          "minimum": 0,
          "maximum": 1,
          "description": "Volume of the error sound (0.0 to 1.0). Note: volume control depends on the system audio player."
        }
      }
    },
    "commands": [
      {
        "command": "ychang-sound.testSound",
        "title": "YChang Sound: Test Sound"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "@types/vscode": "^1.93.0",
    "typescript": "^5.7.3"
  },
  "extensionKind": [
    "ui"
  ]
}
